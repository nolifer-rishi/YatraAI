{
  "buildCommand": "npm install --prefix FRONTEND && npm run build --prefix FRONTEND",
  "outputDirectory": "FRONTEND/dist",
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.py"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
